# Native HANA ↔ BW/4HANA mapping

> **Placeholder — filled in Phase 3.3.**
> One row per object: native HANA object → BW/4HANA equivalent → why this layer exists → what would differ in a real BW/4 system.

Deltas to cover explicitly: delta handling via DTP, InfoObject-based master-data governance, analysis authorizations for row-level security, SDA/SDI/SLT instead of CSV loads, NLS for cold data.
