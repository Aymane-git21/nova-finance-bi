# KPI definitions

> **Placeholder — filled in Phase 2.1.**
> One row per KPI: name, business question, formula, grain, target/threshold, source object, owning role.
> KPIs are defined *before* the schema. The schema exists to serve them, not the other way round.

Planned minimum set: days to close · % manual journal entries · late postings · budget vs actual variance · programme run-rate & EAC · FX impact · forecast accuracy (MAPE) · intercompany mismatches.
