# Development golden rules

> **Placeholder — filled in Phase 8.**
> Naming conventions (`ZI_` / `ZC_` / `CV_` prefixes, layer suffixes), layer rules ("no joins in L1", "no business logic in the UI"), versioning (everything in Git / abapGit), definition of done (peer review, runtime budget, entry in the data dictionary).
